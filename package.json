{
  "name": "tsunagime-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/fetch-withdrawn.mjs && node scripts/prune-withdrawn.mjs",
    "build": "echo build ok"
  }
}