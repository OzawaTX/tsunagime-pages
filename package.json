{
  "name": "tsunagime-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"require('fs').mkdirSync('public',{recursive:true})\" && node scripts/prune-withdrawn.mjs",
    "build": "echo build ok"
  }
}
